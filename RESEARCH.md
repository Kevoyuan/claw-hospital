# AI Agent 常见问题与解决方案研究

> 收集 OpenClaw、Claude Code 及通用 AI Agent 的常见问题、解决方案和调试经验
> 更新时间：2026-03-03

---

## 目录

1. [Claude Code 常见问题与解决方案](#1-claude-code-常见问题与解决方案)
2. [AI Agent 调试技术](#2-ai-agent-调试技术)
3. [代码质量与架构优化](#3-代码质量与架构优化)
4. [安全漏洞与防护](#4-安全漏洞与防护)
5. [最佳实践总结](#5-最佳实践总结)

---

## 1. Claude Code 常见问题与解决方案

### 1.1 API 错误与连接问题

| 错误类型 | 错误代码 | 解决方案 |
|---------|---------|---------|
| API 速率限制 | 429 | 使用指数退避重试，批量请求，或升级到 Pro 计划 |
| 上下文窗口超限 | 400 | 将代码库拆分为更小的逻辑块，使用 `.claudeignore` 排除无关文件 |
| 认证错误 | 401/403 | 重新生成 API 密钥，验证 `CLAUDE_API_KEY` 环境变量 |
| 服务过载 | 529 | 切换到 Sonnet 模型或等待后重试 |
| 请求格式错误 | 400 | 检查 JSON 格式和必需字段 |
| 服务器错误 | 500 | 实施指数退避并重试 |

**调试技巧：**
- 使用 `/compact` 命令可减少 50% 处理时间
- 长时间对话后使用 `/clear` 开始新会话
- 为新话题开启新会话避免上下文污染

### 1.2 安装与设置问题

| 问题 | 解决方案 |
|-----|---------|
| `command not found: claude` | 检查 PATH 配置，添加 `~/.local/bin` 到 PATH |
| 权限错误 | 修复 npm 权限或使用 nvm |
| VS Code 扩展不工作 | 手动安装扩展，重启 VS Code，先运行 `/ide` 命令 |
| 启动卡顿 | 重新安装，检查工具冲突，确保 16GB+ RAM |

**PATH 配置示例：**

```bash
# macOS/Linux (Zsh)
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

### 1.3 性能与输出质量问题

| 问题 | 原因与解决方案 |
|-----|---------------|
| 响应慢 | 保持对话聚焦，避免上传大文件，使用子代理处理复杂分析 |
| 生成"糟糕"代码 | 提供清晰详细的提示，将复杂任务拆分为小步骤 |
| 输出重复/混乱 | 使用 `/clear` 重置上下文，为新话题开新会话 |
| 幻觉 | 在提示中具体说明，使用单元测试和 lint 验证 |

---

## 2. AI Agent 调试技术

### 2.1 常见错误分类

#### 数据相关问题
- **数据质量差/不完整**：导致结果偏差和不可预测的模型性能
- **数据泄露**：测试数据混入训练集，导致过度乐观
- **偏见**：训练数据中的偏见导致歧视性输出
- **数据多样性不足**：模型无法有效学习

#### 模型与算法问题
- **过拟合**：在训练数据上表现好但在新数据上差
- **欠拟合**：未能捕获数据中的复杂模式
- **超参数配置错误**：导致模型性能不佳
- **幻觉**：AI 生成自信但错误的信息
- **边缘情况失败**：只能处理"理想路径"场景
- **规划器无限循环**：成功标准模糊导致重复

#### 系统与集成问题
- **工具调用错误**：AI 代理错误调用外部工具
- **内存与上下文管理**：长对话中信息丢失或内存污染
- **级联错误传播**：多步骤过程中初始错误放大
- **安全漏洞引入**：生成功能正确但有安全问题的代码

### 2.2 调试技术与解决方案

#### 系统化调试流程
1. **错误识别**：通过性能指标、用户反馈或监控工具检测异常
2. **根因分析**：确定错误来源于数据、算法还是部署
3. **修复与验证**：应用修正并用测试数据集验证
4. **监控**：部署后持续跟踪模型性能

#### AI 辅助调试
- **推理优先提示**：先让 AI 分析可能原因，再编写代码
  ```
  "列出5-7个可能的原因并为每个原因提出诊断方案，暂时不写代码"
  ```
- **自动化修复循环**：让 AI 迭代修复和测试
- **添加日志和调试语句**：让 AI 注入日志以打印输入 payload、验证输出等

#### 可观测性与监控
- **全面日志记录**：捕获每个代理操作、通信和工具使用
- **分布式追踪**：系统化记录多代理工作流中的交互
- **实时监控**：实施持续检查确保模型稳定运行

#### 增量调试技巧
- 将复杂错误拆分为更小的问题
- 使用单元测试和逐步验证
- 实现健壮的错误处理机制
- 在代码中而非提示中强制执行行为

---

## 3. 代码质量与架构优化

### 3.1 减少上下文需求

**核心原则**：将代码组织为问题域，而非技术层。

| 按技术层组织 | 按问题域组织 |
|------------|-------------|
| `/controllers`, `/services`, `/models` | `/auth`, `/payments`, `/notifications` |

**好处**：
- AI 只需关注当前任务相关的文件
- 减少上下文污染
- 提高代码可维护性

### 3.2 AI 作为架构审查工具

> 如果 AI 难以理解你的代码，你的同事可能也难以理解。

**实践技巧**：
- 将 API 设计为意图明确：创建专用端点而非通用端点
- 使问题域边界明确：包结构、命名约定、API 边界
- 使用清晰、直观的命名（AI 也能理解）

### 3.3 提示工程最佳实践

| 问题 | 解决方案 |
|-----|---------|
| 指令模糊 | 具体明确，提供细节 |
| 上下文管理 | 使用 `.claudeignore`，预处理大文件 |
| 输出结构混乱 | 使用 XML 标签分隔指令和上下文 |
| 复杂任务 | 拆分为步骤，使用提示链 |
| 推理不佳 | 添加"逐步思考"引导 |

---

## 4. 安全漏洞与防护

### 4.1 已知安全风险

- **远程代码执行 (RCE)**：恶意仓库的特殊配置文件可执行任意 shell 命令
- **API 密钥盗窃**：通过配置错误或恶意代码窃取凭证

### 4.2 安全防护措施

1. **谨慎克隆仓库**：尤其来源不信任时
2. **保护 API 密钥**：存储在环境变量或安全存储服务，绝不明文存储在代码中
3. **用户确认提示**：确保工具和服务在明确批准前不初始化
4. **企业证书配置**：如需代理 TLS 检查，设置 `NODE_EXTRA_CA_CERTS`

---

## 5. 最佳实践总结

### 开发工作流

- [ ] 新话题开启新会话
- [ ] 使用 `/compact` 管理长对话
- [ ] 将复杂任务拆分为小步骤
- [ ] 先推理后编码（推理优先提示）
- [ ] 为好的行为编写测试用例

### 架构设计

- [ ] 按问题域组织代码结构
- [ ] API 设计意图明确
- [ ] 保持问题域边界清晰
- [ ] 减少不必要的上下文

### 调试与监控

- [ ] 实施全面日志记录
- [ ] 添加调试日志和检查点
- [ ] 使用子代理进行复杂分析
- [ ] 建立增量调试循环
- [ ] 部署前确保可观测性工具就位

### 安全意识

- [ ] API 密钥存环境变量
- [ ] 谨慎处理外部仓库
- [ ] 实施人机协作审查
- [ ] 定期更新依赖和工具

---

## 参考资源

- [Claude Code 官方故障排除文档](https://code.claude.com/docs/en/troubleshooting)
- [Claude Code 提示工程最佳实践](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)
- [AI Agent 调试指南](https://galileo.ai/blog/debug-ai-agents)
- [为什么 Claude Code 写出糟糕的代码及如何修复](https://thrawn01.org/posts/why-claude-code-keeps-writing-terrible-code---and-how-to-fix-it)
